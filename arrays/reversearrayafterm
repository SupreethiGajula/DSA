//c++ program to reverse a given array after a given position or index



void reverseArray(vector<int> &arr , int m){
    // Write your code here       
    int i = m+1;
    int j = arr.size()-1;
    while (i < j) {
      swap(arr[i++], arr[j--]);
    }
}
